import styled from 'styled-components';

export const BlockquoteContainer = styled.div `
    width: 100%;
    height: 50px;

    blockquote {
        text-align: center;
        margin: auto;
    }

    span {
        font-weight: 700;
    }
`